{
  "name": "kev-cms",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "test":            "vitest run",
    "test:watch":      "vitest",
    "test:integration": "vitest run --config vitest.integration.config.js"
  },
  "devDependencies": {
    "vitest": "^2.1.0"
  }
}
